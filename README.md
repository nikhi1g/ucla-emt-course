# UCLA EMT Course

Clean static Hello World site deployed with GitHub Pages.

No app framework or build step is required.

## Local preview

Open `index.html` in a browser.

## Deploy

The GitHub Actions workflow in `.github/workflows/pages.yml` uploads the repository root and publishes it to GitHub Pages.
